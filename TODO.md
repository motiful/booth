# Booth TODO

## Next
- [x] `booth watch <name>` CLI command
- [x] `booth info` CLI command
- [x] `booth ps` CLI command
- [x] NPM publishConfig setup
- [ ] NPM publish (needs `npm login` + `@motiful` scope)
- [ ] OpenClaw compatibility research

## Future
- [ ] `booth setup --uninstall`
- [ ] Deck auto-JSONL path discovery optimization
- [ ] `booth logs <name>` — tail JSONL output
- [ ] Better error messages when tmux not installed
